Escribir un archivo en HTML5 denominado Ejercicio13 (con extensión .html) que referencie a un
archivo CSS denominado Ejercicio13 (con extensión .css) y también debe referenciar a un
archivo en JavaScript (extensión .js).
Se debe escribir una aplicación que use el API FILE de HTML5 que cargue archivos desde la
máquina cliente y que visualice sus propiedades (tamaño, tipo, etc.).
En el caso de que se seleccionen archivos de tipo:
 Texto
 JSON
 XML
Deben mostrarse íntegramente por pantalla.
